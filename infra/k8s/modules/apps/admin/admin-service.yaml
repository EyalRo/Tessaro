apiVersion: v1
kind: Service
metadata:
  name: admin-web
  labels:
    app.kubernetes.io/name: admin-web
    app.kubernetes.io/part-of: tessaro-admin
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: admin-web
    app.kubernetes.io/part-of: tessaro-admin
  ports:
    - name: http
      port: 80
      targetPort: http
